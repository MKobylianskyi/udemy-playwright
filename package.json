{
  "name": "udemy-playwright",
  "version": "1.0.0",
  "description": "testing project",
  "main": "index.js",
  "scripts": {
    "report": "npx playwright show-report",
    "test": "playwright test --config=main.config.ts --project=Chromium --repeat-each 1  --workers 3",
    "debug": "playwright test --config=debug.config.ts --project=Chromium --repeat-each 1 --workers 3",
    "test:browser": "npx playwright open --timezone='Europe/Rome' --lang='it-IT' google.com"
  },
  "author": "Mykhailo",
  "license": "ISC",
  "devDependencies": {
    "prettier": "2.6.2"
  },
  "dependencies": {
    "@faker-js/faker": "7.1.0",
    "@playwright/test": "1.21.1"
  }
}
