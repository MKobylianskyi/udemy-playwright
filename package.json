{
  "name": "udemy-playwright",
  "version": "1.0.0",
  "description": "testing project",
  "main": "index.js",
  "scripts": {
    "test:headless": "playwright test --config=main.config.ts --project=Chromium --repeat-each 1",
    "test:debug": "playwright test --config=debug.config.ts --project=Chromium --repeat-each 1",
    "test:browser": "npx playwright open --timezone='Europe/Rome' --lang='it-IT' google.com"
  },
  "author": "Mykhailo",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^2.6.2"
  },
  "dependencies": {
    "@playwright/test": "^1.21.1"
  }
}
